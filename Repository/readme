Questi file vanno nel pc con il database. Come si diceva nella tesi questa macchina va protetta perché comunica con http come serve ma la connessione non è crittografata. Si può usare una connessione diretta ethernet, una vpn o una rete wifi a parte (va bene anche una rete wifi sicura dove non ci sono persone che possano avere cattive intenzioni). Se si vuole la crittografia ssl bisogna pagare un po' per l'estensione (per questo per la tesi non l'ho inserita). In realtà ho visto in ospedale che vivono su computer con sistemi operativi obsoleti e stanno comunque connessi alla rete aziendale ma hanno appunto la rete aziendale super chiusa specifica per le macchine hanno chiuso tutte le porte di comunicazione (anche quelle usb e seriali!). È una pezza, ma funziona. nella cartella principale vanno messi i due file senza cartella (quindi su sistemi unix equivale a dire /home/<nomeutente>, oppure basta un ~). Una è la look_up_table identica a quella che ha il webserver. Si potrebbe pensare pure di esporre un servizio nel webserver (magari eliminado gli strati "alti" della pila iso osi e usando solo il tcp) per condividere il file json così che se si modifica un file non bisogna fare copia e incolla. L'altro file è l'esportazione delle tabelle del database (guida per l'import  https://www.digitalocean.com/community/tutorials/how-to-import-and-export-databases-in-mysql-or-mariadb). Non credo che abbia importato anche gli utenti e i permessi e non credo sia stato crittografato. In ogni caso ci sono username e password negli script JavaScript nella cartella Mirth. Nella cartella Resources, che è vuota, andranno messi i segnali, per cui è bene metterla già così (non ho mai provato, dovrebbe crearla se non esiste, ma meglio prevenire). Se si vuole la mia macchina virtuale lubuntu che ho usato per la tesi posso passarla ma non la posso caricare qui perché sono circa 8 GB e non la carico su Drive perché altrimenti mi occupa metà del mio spazio.
